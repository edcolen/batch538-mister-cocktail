<nav class="navbar navbar-light bg-light">
  <div class="container-fluid">
    <div class="col-12 col-sm-5 text-wrap text-center">
      <%= link_to "Marcelinho's Hipster Cocktails", root_path, class:"ms-3 page-title text-success" %>
    </div>
    <div class="col d-flex justify-content-center flex-wrap">
      <div class="row p-1 align-self-center">
        <%= button_to "create a new cocktail", new_cocktail_path, method: :get, class: "btn btn-success"%>
      </div>
      <div class="row p-1 align-self-center">

        <!-- usando simple form sem um model e com uma url específica -->
        <%= simple_form_for :search, url: cocktails_path, method: "GET", html: { class: "d-flex" } do |f| %>
          <div class="input-group">
            <%= f.input :name, label: false, input_html: { placeholder: "Find a cocktail", value: nil, class: "form-control me-2" } %>
            <%= f.submit "search", class: "btn btn-outline-success" %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</nav>
